---
name: review
version: 2.1.0
description: |
  设计审查与优化模块。对现有界面进行全方位质量审查，涵盖视觉层次、信息架构、认知负荷、可访问性、性能、响应式、主题、排版、间距、布局、对齐、色彩、动效、微交互、UX文案、错误状态、边缘情况、Web Interface Guidelines、i18n和可复用设计系统。
  由 super-frontend-design 主入口路由调用。
  基于 impeccable + web-design-guidelines 重新实现。
  v1.1.0 新增反AI味专项审查。v1.2.0 新增 Web Interface Guidelines 合规性审查维度。v1.3.0 新增设计灵魂追溯检查 + Design Brief 对照验证 + 全局画像禁令。v2.0.0: 新增反AI硬检查(§0.5-0.6)·Typography硬检查(§2.16)·可访问性自动扫描(§2.17)·审查→设计返修回路; v2.1.0: 企业设计系统参考目录集成
context: fork
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, WebFetch
---

### 1. 概述

设计审查子技能是 super-frontend-design 的核心质量保障模块，负责对现有界面进行全方位 UI/UX 质量审查，并提供可执行的改进建议。

**审查定位：**
- 全方位 UI/UX 质量审查，不仅仅是视觉检查
- 从用户视角出发，覆盖从视觉呈现到交互体验的完整链路
- 输出结构化审查报告，包含具体改进建议和优先级排序
- **v1.3.0 新增：必须对照 Design Brief 验证设计是否满足用户原始需求**

**适用场景：**
- 网站（营销站、内容站、电商站）
- 落地页（Landing Page）
- 仪表板（Dashboard）
- 产品 UI（SaaS 应用、管理后台）
- 应用壳（App Shell）
- 组件（按钮、表单、卡片、表格等）
- 表单（注册、登录、设置等）
- 设置页面
- 引导流程（Onboarding）
- 空状态页面

**核心能力：**
- 19 个审查维度，覆盖 UI/UX 全部关键领域
- 每个维度包含具体检查项和量化标准
- 问题标记采用三级严重度（高/中/低）
- 审查后可直接执行改进，形成闭环

**基于 impeccable 重新实现：**
- 保留了 impeccable 的全面审查维度
- 增加了量化评分体系（0-100分）
- 增加了结构化报告格式
- 增加了改进执行闭环

---

### 0. 审查前置：设计灵魂追溯（Design Brief 对照验证）

> ⚠️ **v1.3.0 新增：在执行 15 维度技术审查之前，必须先验证设计是否符合用户原始需求。**

#### 检查表（必须逐项确认）

| # | 检查项 | 验证方式 | 不合格处理 |
|:---:|------|---------|-----------|
| 1 | **品牌人格体现** | 设计是否让用户感受到 Design Brief 中指定的品牌人格？ | 🔴 严重：设计方向错误，需重新设计 |
| 2 | **情绪板匹配** | 3 个关键词是否在视觉中体现？（如"科技感+未来感+冷静"） | 🔴 严重：情绪不对，调整配色/材质 |
| 3 | **视觉论文一致性** | 最终产出是否与 creative 阶段的 5 个方向决策一致？ | 🟡 中等：标注偏差并建议修正 |
| 4 | **设计禁忌合规** | 逐项检查用户列出的所有设计禁忌是否被违反？ | 🔴 严重：违禁出现，必须移除 |
| 5 | **企业参考风格** | 是否体现了用户选择的企业参考风格特征？ | 🟡 中等：标注缺失 |
| 6 | **响应式与设计类型一致** | 设计类型是后台 → 检查信息密度；是落地页 → 检查视觉冲击 | 🔴 严重：类型不匹配需重设计 |
| 7 | **暗色模式与调研一致** | 用户说要暗色 → 必须检查暗色实现；用户说不要 → 不应出现 | 🟡 中等 |
| 8 | **交互复杂度与调研一致** | 用户说静态展示 → 不应有复杂交互；用户说完整应用 → 必须有路由/状态管理 | 🟡 中等 |

#### 全局画像禁令

- ❌ 不从用户全局画像中读取偏好看设计是否"符合"
- ❌ 不因为"用户上次喜欢暗色"就判定当前亮色设计不合格
- ✅ 审查的「期望」只能来自 Design Brief 和本次会话上下文

### 0.5 去 AI 味硬检查（七宗罪 — 任何一项不通过则审查不通过）

> 加载 `references/anti-ai-slop.md`，逐项检查以下 7 条。任一失败 → 审查 blocked，返回 design 阶段。

| # | 检查项 | 检测方式 | 失败动作 |
|:---:|------|---------|---------|
| 1 | **AI 默认色检测** | Grep 搜索 `#6366f1\|#4f46e5\|#4338ca\|#3730a3\|#8b5cf6\|#7c3aed\|#a855f7` | 🔴 Blocked · 列出所有违规 hex 值所在文件和行号 |
| 2 | **两色渐变 Hero** | Grep 搜索渐变模式 `linear-gradient.*#8b5cf6.*#3b82f6` 等 | 🔴 Blocked · 列出所有渐变组合 |
| 3 | **Emoji 功能图标** | Grep 搜索 emoji 在 h1-h6/button/li/icon class 中 | 🔴 Blocked · 列出所有 emoji 位置 |
| 4 | **h1/h2 硬编码字体** | Grep 搜索 `font-family:\s*(Inter\|Roboto\|system-ui)` 在 h1/h2 范围内 | 🔴 Blocked · 必须改为 var(--font-display) |
| 5 | **圆角卡片+彩色左边框** | 检查 `.card` 或类似元素同时有 border-radius 和 border-left 差异化颜色 | 🔴 Blocked · 二选一移除 |
| 6 | **假指标** | Grep 搜索 "× faster\|× 更\|% uptime\|% 在线\|百万" | 🔴 Blocked · 列出所有假指标 |
| 7 | **填充文案** | Grep 搜索 "lorem ipsum\|功能一\|功能二\|功能三\|placeholder" | 🔴 Blocked · 替换为真实文案或 [待填充] |

### 0.6 软规则检查（P1 — 警告但不会 Block）

| # | 检查项 | 检测方式 |
|:---:|------|---------|
| 8 | **标准序列** | 检查 section 顺序是否为 Hero→Features→Pricing→FAQ→CTA |
| 9 | **占位图片 CDN** | Grep 搜索 `unsplash\|placehold.co\|placekitten\|picsum.photos` |
| 10 | **裸 hex 值** | 统计 `:root` 外的 hex 值是否超 12 个 |
| 11 | **Accent 滥用** | 统计 `var(--accent)` 每屏使用次数是否 >2 |

#### 设计灵魂评分

基于以上 8 项检查，给出设计灵魂匹配度评分（0-100分）：

| 得分 | 等级 | 说明 |
|:---:|:---:|------|
| 90-100 | A+ | 完美体现用户所有调研需求 |
| 75-89 | A | 基本满足，有少量偏差 |
| 60-74 | B | 存在明显偏差，需要调整 |
| <60 | C | 设计方向与调研结果严重不符，建议重新设计 |

---

### 2. 审查维度（完整列出所有维度）

#### 2.1 视觉审查

视觉审查关注界面的视觉呈现质量，确保信息能够有效地通过视觉传递给用户。

**2.1.1 视觉层次**

检查项：
- 是否有清晰的视觉层次？最重要的元素是否最突出？
- 标题是否比正文更大更粗？
- CTA 按钮是否比普通按钮更醒目？
- 关键信息是否通过大小、颜色、位置建立了明确的优先级？
- 是否存在视觉竞争（多个元素争夺注意力）？

量化标准：
- 标题与正文字号比应 ≥ 1.5:1
- CTA 按钮与普通按钮面积比应 ≥ 1.3:1
- 首屏应有且仅有 1 个最突出的视觉焦点

**2.1.2 信息架构**

检查项：
- 内容是否按逻辑组织？用户能否快速找到所需信息？
- 导航结构是否清晰？层级是否不超过 3 层？
- 相关内容是否分组？不相关内容是否分离？
- 是否遵循了 F 型或 Z 型阅读模式？
- 信息密度是否适当？

量化标准：
- 导航层级 ≤ 3 层
- 每个分组包含 3-7 个项目（Miller's Law）
- 首屏可见的核心操作 ≤ 5 个

**2.1.3 认知负荷**

检查项：
- 界面是否过于复杂？是否需要简化？
- 用户完成核心任务需要多少步骤？
- 是否有不必要的决策点？
- 是否利用了渐进式披露（Progressive Disclosure）？
- 信息是否分块呈现？

量化标准：
- 核心任务完成步骤 ≤ 5 步
- 同一屏幕的决策选项 ≤ 7 个
- 表单字段数 ≤ 核心所需 + 2

---

#### 2.2 可访问性审查

可访问性审查确保界面对所有用户可用，包括残障用户。

**2.2.1 对比度**

检查项：
- 文本对比度是否满足 WCAG AA（4.5:1）？
- 大文本（≥18pt 或 ≥14pt 粗体）对比度是否满足 3:1？
- 非文本元素（图标、边框）对比度是否满足 3:1？
- 暗色模式下的对比度是否独立验证？

量化标准：
- 正文文本对比度 ≥ 4.5:1（WCAG AA）
- 大文本对比度 ≥ 3:1（WCAG AA）
- 建议达到 WCAG AAA 标准（正文 ≥ 7:1）

**2.2.2 键盘导航**

检查项：
- 是否支持完整的键盘导航？
- Tab 顺序是否符合逻辑？
- 是否有键盘陷阱（Keyboard Trap）？
- 焦点是否可见且明显？
- 是否支持快捷键？

量化标准：
- 所有交互元素可通过键盘访问
- 焦点环对比度 ≥ 3:1
- 无键盘陷阱

**2.2.3 屏幕阅读器**

检查项：
- 是否有适当的 ARIA 标签？
- 图片是否有 alt 文本？
- 表单是否有关联的 label？
- 动态内容是否有 aria-live 区域？
- 页面结构是否使用语义化 HTML？

量化标准：
- 所有 `<img>` 有非空 alt 或 `alt=""`
- 所有 `<input>` 有关联 `<label>` 或 `aria-label`
- 使用 `<nav>`、`<main>`、`<aside>`、`<footer>` 等语义标签

**2.2.4 颜色依赖**

检查项：
- 是否仅靠颜色传达信息？
- 错误状态是否同时使用图标/文本和颜色？
- 链接是否通过下划线或背景区分（不仅仅是颜色）？
- 图表是否使用形状/图案辅助颜色区分？

量化标准：
- 无任何信息仅通过颜色传达
- 错误状态至少使用颜色 + 图标/文本双重编码

---

#### 2.3 性能审查

性能审查关注界面的加载和运行时性能，确保用户体验流畅。

**2.3.1 图片优化**

检查项：
- 是否使用 WebP/AVIF 格式？
- 是否有响应式图片（srcset）？
- 是否有懒加载（loading="lazy"）？
- 图片尺寸是否合适（无过大图片）？
- 是否有占位符/模糊预览？

量化标准：
- 图片格式优先 WebP/AVIF
- 宽度 > 600px 的图片使用 srcset
- 非首屏图片使用 lazy loading
- 单张图片 ≤ 200KB（压缩后）

**2.3.2 字体加载**

检查项：
- 是否使用 font-display: swap？
- 是否预加载关键字体（preload）？
- 字体文件数量是否 ≤ 4 个？
- 是否有字体回退栈？

量化标准：
- 所有 @font-face 使用 font-display: swap
- 关键字体使用 `<link rel="preload">`
- 字体文件数 ≤ 4（2 字体 × 2 字重）

**2.3.3 布局稳定性**

检查项：
- 是否有 CLS（Cumulative Layout Shift）问题？
- 图片/视频是否有明确的 width/height？
- 动态内容是否有预留空间？
- 字体加载是否导致布局偏移？

量化标准：
- CLS ≤ 0.1（Good）
- 所有媒体元素有明确尺寸
- 动态内容使用 min-height 预留空间

**2.3.4 代码分割**

检查项：
- 是否按路由/功能拆分代码？
- 第三方库是否按需加载？
- CSS 是否有未使用的规则？
- JavaScript bundle 大小是否合理？

量化标准：
- 首屏 JS ≤ 100KB（压缩后）
- 路由级别代码分割
- CSS 未使用率 ≤ 20%

---

#### 2.4 响应式审查

响应式审查确保界面在不同设备和屏幕尺寸下可用。

**2.4.1 移动端优先**

检查项：
- 是否从移动端设计开始？
- 移动端布局是否独立于桌面端？
- 触摸交互是否优先考虑？
- 内容优先级是否在移动端重新排列？

量化标准：
- 移动端布局非桌面端简单缩放
- 核心内容在 320px 宽度下可访问

**2.4.2 断点一致性**

检查项：
- 是否使用系统化断点？
- 断点之间是否有布局跳跃？
- 是否覆盖所有主流设备尺寸？

量化标准：
- 使用标准断点：640px / 768px / 1024px / 1280px / 1536px
- 相邻断点间无布局跳跃

**2.4.3 触摸目标**

检查项：
- 是否满足 44×44pt 最小尺寸？
- 触摸目标之间是否有足够间距？
- 小屏幕上按钮是否足够大？

量化标准：
- 可点击元素最小尺寸 ≥ 44×44pt
- 相邻触摸目标间距 ≥ 8pt

**2.4.4 横向模式**

检查项：
- 是否在横向模式下可用？
- 横向布局是否重新排列？
- 是否有横向专属优化？

量化标准：
- 横向模式下无内容溢出或截断
- 横向模式下核心功能可用

---

#### 2.5 主题审查

主题审查确保界面正确使用主题系统，保持视觉一致性。

**2.5.1 暗色模式**

检查项：
- 暗色模式是否完整？所有组件是否适配？
- 暗色模式对比度是否独立验证？
- 暗色模式下图片/图标是否调整？
- 暗色模式切换是否平滑？

量化标准：
- 所有组件有暗色模式变体
- 暗色模式文本对比度 ≥ 4.5:1
- 暗色模式使用 `#1a1a1a` 左右背景而非纯黑 `#000000`

**2.5.2 语义 Token**

检查项：
- 是否使用语义 Token 而非原始 hex 值？
- 是否有硬编码的颜色值？
- Token 引用是否正确？

量化标准：
- CSS 中无硬编码 hex/rgb 值（除原始 Token 定义外）
- 所有颜色引用使用 var(--color-xxx) 格式

**2.5.3 品牌一致性**

检查项：
- 是否遵循品牌规范？
- 品牌色使用是否正确？
- 字体使用是否符合品牌指南？

量化标准：
- 品牌色使用位置和比例符合规范
- 字体配对与品牌指南一致

---

#### 2.6 排版审查

排版审查确保文本内容的可读性和视觉美感。

**2.6.1 字体配对**

检查项：
- 是否限制在两种字体以内？
- 标题字体和正文字体是否形成对比？
- 字体风格是否与品牌调性一致？

量化标准：
- 字体种类 ≤ 2（+1 等宽字体用于代码）
- 标题字体与正文字体有明显风格差异

**2.6.2 行高**

检查项：
- 正文行高是否在 1.5-1.75 之间？
- 标题行高是否在 1.1-1.3 之间？
- 行高是否根据字号调整？

量化标准：
- 正文行高：1.5-1.75
- 标题行高：1.1-1.3
- 小字号行高相对更大

**2.6.3 行长**

检查项：
- 是否限制在 65-75 字符以内？
- 长段落是否有最大宽度限制？
- 不同断点下行长是否合理？

量化标准：
- 正文最大宽度：65-75 字符（约 550-650px）
- 使用 max-width 而非 width 限制

**2.6.4 字重层次**

检查项：
- 标题是否使用 600-700？
- 正文是否使用 400？
- 标签/辅助文本是否使用 500？
- 字重层次是否清晰？

量化标准：
- 标题字重：600-700
- 正文字重：400
- 标签字重：500
- 字重层次 ≥ 3 级

---

#### 2.7 间距审查

间距审查确保界面的空间组织一致和有节奏感。

**2.7.1 间距系统**

检查项：
- 是否使用 4/8dp 增量间距系统？
- 间距值是否来自预定义的间距 Token？
- 是否有随意的间距值？

量化标准：
- 所有间距值为 4 的倍数
- 使用间距 Token 而非魔法数字
- 间距值集：4, 8, 12, 16, 20, 24, 32, 40, 48, 64

**2.7.2 垂直节奏**

检查项：
- 是否有清晰的垂直节奏层级？
- 段落间距是否一致？
- 区块间距是否有规律？

量化标准：
- 段落间距统一（如 16px 或 24px）
- 区块间距 ≥ 2× 段落间距
- 垂直间距遵循倍数关系

**2.7.3 内边距一致性**

检查项：
- 组件内边距是否一致？
- 同类组件的内边距是否统一？
- 内边距比例是否合理？

量化标准：
- 同类组件内边距统一
- 按钮内边距：水平 ≥ 垂直
- 卡片内边距：16-32px

---

#### 2.8 布局审查

布局审查关注界面的整体构图和空间组织。

**2.8.1 构图**

检查项：
- 是否有明确的构图意图？
- 视觉重心是否在预期位置？
- 是否利用了网格系统？

量化标准：
- 使用网格系统（12列或8列）
- 视觉重心与内容优先级一致
- 首屏有明确的视觉焦点

**2.8.2 对齐**

检查项：
- 元素是否正确对齐？
- 是否有一致的对齐基线？
- 文本对齐是否合理？

量化标准：
- 所有元素对齐到网格
- 同行元素基线对齐
- 正文左对齐（非两端对齐）

**2.8.3 留白**

检查项：
- 留白是否有助于聚焦？
- 留白是否充足但不空洞？
- 留白是否有明确目的？

量化标准：
- 关键元素周围留白 ≥ 元素间距
- 首屏留白 ≥ 内容区域的 30%
- 无无意义的空白区域

**2.8.4 密度**

检查项：
- 密度是否适合产品类型？
- 信息密度是否过高或过低？
- 不同区域密度是否合理变化？

量化标准：
- 仪表板：紧凑密度（间距 8-12px）
- 内容站：标准密度（间距 16-24px）
- 营销站：宽松密度（间距 24-48px）

---

#### 2.9 色彩审查

色彩审查确保界面的色彩使用系统化和语义化。

**2.9.1 调色板**

检查项：
- 是否有清晰的调色板系统？
- 色彩数量是否合理？
- 是否有主色、辅助色、强调色的层次？

量化标准：
- 主色 1 个 + 辅助色 1-2 个 + 强调色 1 个
- 中性色阶 ≥ 5 级
- 总色彩数 ≤ 7 种（不含语义色）

**2.9.2 强调色**

检查项：
- 是否有一个主要强调色？
- 强调色使用是否克制？
- 强调色是否用于最重要的交互元素？

量化标准：
- 首屏强调色使用面积 ≤ 10%
- 强调色仅用于 CTA 和关键交互

**2.9.3 语义色**

检查项：
- 错误/成功/警告色是否语义化？
- 语义色是否一致使用？
- 语义色是否与品牌色区分？

量化标准：
- 错误色：红色系（如 #DC3545）
- 成功色：绿色系（如 #28A745）
- 警告色：黄色/橙色系（如 #FFC107）
- 语义色不用于非语义目的

---

#### 2.10 动效审查

动效审查确保界面的动画有意义且性能良好。

**2.10.1 意义**

检查项：
- 每个动效是否表达因果关系？
- 动效是否帮助用户理解界面变化？
- 是否有无意义的装饰性动效？

量化标准：
- 每个动效都有明确的交互目的
- 无纯装饰性动效干扰用户操作

**2.10.2 时长**

检查项：
- 微交互是否在 150-300ms？
- 页面过渡是否在 300-500ms？
- 复杂动画是否 ≤ 1000ms？

量化标准：
- 微交互（hover、press）：150-300ms
- 过渡动画（页面切换、展开收起）：300-500ms
- 复杂动画（onboarding）：≤ 1000ms

**2.10.3 性能**

检查项：
- 是否只使用 transform/opacity？
- 是否避免触发 layout/paint？
- 动画帧率是否 ≥ 60fps？

量化标准：
- 动画属性仅限 transform 和 opacity
- 使用 will-change 提示浏览器优化
- 无卡顿或掉帧

**2.10.4 减少动效**

检查项：
- 是否尊重 prefers-reduced-motion？
- 减少动效模式下是否有替代方案？
- 核心功能是否不依赖动效？

量化标准：
- 实现 @media (prefers-reduced-motion: reduce) 适配
- 减少动效模式下功能完整可用
- 动效关闭时使用即时状态切换

---

#### 2.11 微交互审查

微交互审查关注界面的交互反馈细节。

**2.11.1 悬停状态**

检查项：
- 是否有清晰的悬停反馈？
- 悬停效果是否一致？
- 悬停效果是否在触摸设备上正确处理？

量化标准：
- 所有可点击元素有悬停效果
- 悬停效果类型统一（颜色变化/阴影/缩放）
- 触摸设备不显示悬停效果

**2.11.2 按压状态**

检查项：
- 是否有按压反馈？
- 按压效果是否与悬停效果区分？
- 按压效果是否提供触觉反馈感？

量化标准：
- 所有可点击元素有按压效果
- 按压效果与悬停效果有明显区分
- 建议使用 scale(0.97) 或更深的背景色

**2.11.3 焦点状态**

检查项：
- 是否有可见的焦点环？
- 焦点环颜色是否与背景有足够对比度？
- 焦点环是否使用 :focus-visible 而非 :focus？

量化标准：
- 所有可聚焦元素有焦点环
- 焦点环对比度 ≥ 3:1
- 使用 :focus-visible 避免鼠标点击时显示焦点环

**2.11.4 禁用状态**

检查项：
- 禁用元素是否视觉清晰？
- 禁用状态是否与正常状态有明显区分？
- 禁用元素是否不可聚焦？

量化标准：
- 禁用元素使用 opacity: 0.5 或灰色
- 禁用元素有 cursor: not-allowed
- 禁用元素设置 aria-disabled="true"

---

#### 2.12 UX文案审查

UX文案审查确保界面的文字内容清晰、一致、有帮助。

**2.12.1 清晰性**

检查项：
- 文案是否清晰简洁？
- 是否有行业术语未解释？
- 句子是否简短？

量化标准：
- 句子平均长度 ≤ 20 字
- 无未解释的缩写或术语
- 操作说明 ≤ 2 句话

**2.12.2 一致性**

检查项：
- 术语是否一致？
- 同一操作是否使用相同措辞？
- 称呼是否统一（你/您）？

量化标准：
- 同一概念使用统一术语
- 同一操作使用统一动词
- 全文称呼统一

**2.12.3 行动导向**

检查项：
- 按钮文案是否行动导向？
- 是否使用动词开头？
- 是否明确告知用户将发生什么？

量化标准：
- 按钮文案使用动词开头（如"保存"而非"确定"）
- 按钮文案描述具体操作（如"删除项目"而非"确认"）

**2.12.4 错误信息**

检查项：
- 错误信息是否包含恢复路径？
- 错误信息是否友好？
- 错误信息是否具体？

量化标准：
- 错误信息包含恢复建议
- 错误信息使用友好语气
- 错误信息具体描述问题（如"密码至少8位"而非"输入无效"）

---

#### 2.13 错误状态审查

错误状态审查确保界面在异常情况下仍然可用和友好。

**2.13.1 空状态**

检查项：
- 是否有有意义的空状态？
- 空状态是否有引导操作？
- 空状态是否有插图/图标？

量化标准：
- 所有列表/表格有空状态设计
- 空状态包含引导操作按钮
- 空状态使用插图或图标增强理解

**2.13.2 加载状态**

检查项：
- 是否有骨架屏/加载指示器？
- 加载状态是否告知用户等待时间？
- 长时间加载是否有进度指示？

量化标准：
- 数据加载使用骨架屏而非旋转图标
- 加载时间 > 2s 显示进度
- 加载时间 > 10s 显示百分比

**2.13.3 错误边界**

检查项：
- 是否有错误边界处理？
- 错误边界是否有恢复操作？
- 错误边界是否记录错误信息？

量化标准：
- 组件级错误边界
- 错误边界提供重试操作
- 错误信息记录到日志系统

---

#### 2.14 边缘情况审查

边缘情况审查确保界面在极端场景下仍然可用。

**2.14.1 长文本**

检查项：
- 长文本是否正确处理？
- 是否有文本截断/换行策略？
- 长标题/长名称是否破坏布局？

量化标准：
- 长文本使用 text-overflow: ellipsis 或 word-break
- 标题最大行数限制（如 2 行）
- 表格长文本使用截断 + tooltip

**2.14.2 极端数据**

检查项：
- 极端数据值是否正确显示？
- 数字格式化是否合理？
- 零值/负值/极大值是否处理？

量化标准：
- 大数字使用千分位分隔符
- 金额使用货币格式
- 百分比保留合理小数位
- 零值有特殊显示（如 "—" 或 "0"）

**2.14.3 国际化**

检查项：
- 是否支持 i18n？
- 文本方向是否支持 RTL？
- 日期/时间格式是否本地化？
- 货币格式是否本地化？

量化标准：
- 文本使用 i18n key 而非硬编码
- 布局支持 RTL 方向
- 日期/时间使用本地化格式
- 货币使用本地化符号和格式

---

#### 2.15 Web Interface Guidelines 合规性审查

Web Interface Guidelines 合规性审查确保界面遵循现代 Web 平台的设计规范和最佳实践，与原生平台体验保持一致性。

**2.15.1 平台一致性**

检查项：
- 是否遵循目标平台的设计语言？（macOS/iOS 的 Human Interface Guidelines、Material Design、Fluent Design）
- 表单控件是否使用原生语义化元素（`<input>`、`<select>`、`<button>`）而非完全自定义？
- 滚动行为是否符合平台惯例？（惯性滚动、滚动条样式）
- 右键菜单、拖拽行为是否符合用户平台预期？

量化标准：
- 优先使用原生 HTML 语义化控件，仅在必要时自定义
- 表单元素保留原生焦点环和键盘行为
- 滚动体验与平台一致（macOS 平滑滚动、Windows 标准滚动）

**2.15.2 浏览器兼容性**

检查项：
- 是否支持主流浏览器的最近 2 个版本？（Chrome、Firefox、Safari、Edge）
- 是否使用了无前缀或带前缀的 CSS 属性？（flex、grid、clip-path）
- 是否使用了渐进增强（Progressive Enhancement）策略？
- 是否对旧浏览器有降级方案？

量化标准：
- 核心功能在所有目标浏览器中可用
- 使用 `@supports` 为高级特性提供条件支持
- 不使用仅被单个浏览器支持的实验性 API

**2.15.3 系统 UI 集成**

检查项：
- 是否尊重用户的系统偏好设置？（暗色模式、减少动效、高对比度）
- 是否使用了 `color-scheme` 让浏览器 UI 元素（滚动条、表单控件）适配主题？
- 是否使用了 `@media (prefers-contrast: high)` 支持高对比度模式？
- 是否正确处理了系统字体设置？

量化标准：
- 实现 `prefers-color-scheme` 适配（不仅手动切换，也响应系统设置）
- 实现 `prefers-reduced-motion` 适配
- 实现 `prefers-contrast` 适配
- 使用 `-apple-system`、`Segoe UI`、`Roboto` 等系统字体栈

**2.15.4 交互模式合规性**

检查项：
- 链接是否可识别为链接？（下划线、颜色区分、hover 状态）
- 按钮是否可识别为按钮？（明确的按钮样式，非纯文本）
- 表单提交是否遵循标准 HTTP 行为？
- 是否避免劫持系统快捷键？（Cmd/Ctrl+K、Cmd/Ctrl+F、Esc）
- 模态框是否可通过 Esc 关闭？
- 是否支持浏览器返回按钮导航？

量化标准：
- 链接必须有视觉区分（下划线或颜色 + hover）
- 按钮必须有明确的按钮样式（背景色、边框、圆角）
- 不劫持系统级快捷键
- 模态框/弹层支持 Esc 关闭和点击背景关闭
- SPA 应用正确处理浏览器历史（Back/Forward）

**2.15.5 表单最佳实践**

检查项：
- 是否使用了正确的 input type？（email、tel、number、date）
- 是否使用了 autocomplete 属性帮助浏览器自动填充？
- 是否使用了正确的 label 关联？（`<label for="">` 或隐式包裹）
- 是否阻止了浏览器的默认表单验证样式？（如需要，是否提供了替代方案）
- 文件上传是否支持拖拽？

量化标准：
- 所有输入使用语义化 type
- 表单字段使用 `autocomplete` 属性
- 每个输入都有关联 label
- 自定义验证样式覆盖或补充浏览器默认验证

**2.15.6 安全与隐私**

检查项：
- 密码输入是否使用了 `type="password"`？
- 敏感表单是否在 HTTPS 环境下提交？
- 是否使用了 `autocomplete="new-password"` 避免浏览器自动填充密码？
- 第三方嵌入（iframe、脚本）是否使用了适当的沙箱和 referrer 策略？

量化标准：
- 敏感输入使用正确的 type 和 autocomplete
- 外部资源使用 `rel="noopener noreferrer"`
- iframe 嵌入使用 `sandbox` 属性限制权限

---

#### 2.16 Typography 硬检查（基于 references/typography.md）

> 加载 `references/typography.md`，逐项检查以下 8 条。

| # | 检查项 | 检测方式 | 不通过 |
|:---:|------|---------|:---:|
| T1 | ALL CAPS letter-spacing | Grep `text-transform:.*upper` 检查是否有 `letter-spacing` | 🔴 |
| T2 | Display tracking | 检查 ≥48px 元素是否有 `letter-spacing: -0.02em` 或更紧 | 🟡 |
| T3 | Body line-height | 检查 15-18px 文本 `line-height` ≥ 1.5 | 🟡 |
| T4 | 行宽 | 检查 body copy `max-width` ≤ 75ch | 🟡 |
| T5 | Typeface 数量 | 统计不同 font-family 第一字体 ≤ 2 | 🟡 |
| T6 | system-ui 标题 | Grep h1-h6 中 `font-family:.*system-ui`（非 var(--font-display)） | 🔴 |
| T7 | Weight 数量 | 统计 font-weight 种类 ≤ 3 | 🟢 |
| T8 | justify | Grep `text-align: justify` | 🔴 |

**检查结果格式**：

```markdown
## Typography 检查报告

| # | 状态 | 位置 | 详情 |
|:---:|:---:|------|------|
| T1 | ✅ PASS | — | ALL CAPS 均含 letter-spacing ≥ 0.06em |
| T2 | ⚠️ FAIL | index.html:45 | `<h1>` 48px 缺少负 tracking |
| ... | ... | ... | ... |
```

---

#### 2.17 可访问性自动扫描

> 基于 Grep 模式匹配的可访问性硬检查。与 §2.2 的可访问性语义审查互补 — §2.2 做定性判断，§2.17 做自动化定量检测。

| # | 检查项 | 检测方式 | 不通过 |
|:---:|------|---------|:---:|
| A1 | 缺少 alt 属性的图片 | Grep `<img(?![^>]*alt=)` | 🔴 |
| A2 | input 缺少可访问标签 | Grep `<input` 且无 `aria-label\|aria-labelledby` 且上下文无 `<label for=` | 🔴 |
| A3 | div 模拟按钮无 ARIA | Grep `<div[^>]*onClick` 检查是否有 `role="button"` 和 `tabindex` | 🔴 |
| A4 | html 根元素缺少 lang | Grep `<html(?![^>]*lang=)` | 🔴 |
| A5 | 低对比度颜色组合 | 检查 foreground hex 和 background hex（或 CSS 变量引用）的 WCAG 对比度 ≥ 4.5:1 | 🟡 |
| A6 | 缺少 :focus-visible 样式 | 统计 `:focus-visible` 出现次数，应至少覆盖所有交互元素类型的 80% | 🟡 |
| A7 | 纯颜色传达信息 | Grep `.error\|.success\|.warning` 类元素是否同时有图标/文本（非仅颜色） | 🟡 |
| A8 | 表单缺少 submit button | 检查 `<form` 内是否有 `type="submit"` 元素 | 🟡 |

**检查结果格式**：

```
## 可访问性自动扫描报告

| # | 状态 | 位置 | 详情 |
|:---:|:---:|------|------|
| A1 | ✅ PASS | — | 所有 img 有 alt 属性 |
| A2 | ⚠️ FAIL | form.html:23 | `<input type="email">` 缺少 aria-label 和 label 关联 |
| ... | ... | ... | ... |
```

> 注意：Grep 检测可能存在假阳性（动态生成的 HTML、JSX 中的特殊语法）。标记为 FAIL 的项需人工确认。

---

### 3. 审查流程

审查遵循严格的流程，确保系统性和可重复性：

#### 步骤 1：读取目标文件/代码

- 使用 Read 工具读取目标文件
- 使用 Glob 工具发现相关文件
- 使用 Grep 工具搜索特定模式
- 识别制品类型（网站/应用/组件/文档）
- 确定审查范围（全量审查/增量审查）

#### 步骤 2：按维度逐项审查

- 按照 19 个维度的顺序进行审查
- 每个维度对照检查项逐项检查
- 记录发现的问题
- 对问题进行初步分级

#### 步骤 3：标记问题（高/中/低严重度）

**高严重度（❌）：**
- 影响核心功能可用性的问题
- 可访问性违规（WCAG AA 不通过）
- 严重的视觉/布局错误
- 性能严重问题（CLS > 0.25、LCP > 4s）

**中严重度（⚠️）：**
- 影响用户体验但不阻塞功能的问题
- 可访问性改进建议（WCAG AAA 目标）
- 视觉一致性问题
- 性能优化机会

**低严重度（💡）：**
- 锦上添花的改进
- 最佳实践建议
- 微小的视觉优化
- 代码质量改进

#### 步骤 4：提供具体改进建议

- 每个问题附带具体的改进建议
- 改进建议包含代码示例（如适用）
- 改进建议指明优先级
- 改进建议评估实施难度

#### 步骤 5：输出审查报告

- 按照标准格式输出审查报告
- 包含总体评分和维度评分
- 包含问题列表和改进优先级
- 报告可直接用于任务跟踪

---

### 4. 审查报告格式

审查报告遵循以下标准格式：

```markdown
# 设计审查报告

**审查目标：** [文件/项目名称]
**审查日期：** [YYYY-MM-DD]
**审查范围：** [全量/增量]

## 总体评分：X/100（各维度加权平均）

> 每个维度 0-10 分，最终总分 = round(各维度得分均值 × 10)，满分 100。

### 评分等级
- 🟢 80-100：优秀，无需改进
- 🟡 65-79：良好，有改进空间
- 🟠 50-64：一般，需要改进
- 🔴 0-49：较差，需要重大改进

## 维度评分

| 维度 | 评分 | 状态 | 关键问题数 |
|------|------|------|-----------|
| 视觉层次 | X/10 | ✅/⚠️/❌ | N |
| 信息架构 | X/10 | ✅/⚠️/❌ | N |
| 认知负荷 | X/10 | ✅/⚠️/❌ | N |
| 可访问性 | X/10 | ✅/⚠️/❌ | N |
| 性能 | X/10 | ✅/⚠️/❌ | N |
| 响应式 | X/10 | ✅/⚠️/❌ | N |
| 主题 | X/10 | ✅/⚠️/❌ | N |
| 排版 | X/10 | ✅/⚠️/❌ | N |
| 间距 | X/10 | ✅/⚠️/❌ | N |
| 布局 | X/10 | ✅/⚠️/❌ | N |
| 色彩 | X/10 | ✅/⚠️/❌ | N |
| 动效 | X/10 | ✅/⚠️/❌ | N |
| 微交互 | X/10 | ✅/⚠️/❌ | N |
| UX文案 | X/10 | ✅/⚠️/❌ | N |
| 错误状态 | X/10 | ✅/⚠️/❌ | N |
| 边缘情况 | X/10 | ✅/⚠️/❌ | N |
| Web Interface Guidelines | X/10 | ✅/⚠️/❌ | N |
| Typography 硬检查 | X/10 | ✅/⚠️/❌ | N |
| 反AI味 | X/10 | ✅/⚠️/❌ | N |
| 设计灵魂 | X/10 | ✅/⚠️/❌ | N |
| 可访问性扫描 | X/10 | ✅/⚠️/❌ | N |

**状态说明：** ✅ 通过（≥8分） / ⚠️ 需改进（5-7分） / ❌ 不通过（<5分）

## 问题列表

### 问题 1: [问题标题]

- **严重度：** 高 ❌ / 中 ⚠️ / 低 💡
- **维度：** [对应维度名称]
- **位置：** [文件:行号] 或 [组件:属性]
- **描述：** [问题详细描述]
- **当前代码：**
  ```html/css/js
  // 当前有问题的代码
  ```
- **建议修改：**
  ```html/css/js
  // 建议的修改代码
  ```
- **改进效果：** [修改后的预期效果]

### 问题 2: [问题标题]
...

## 改进优先级

### 🔴 高优先级（必须修复）
1. [最高优先级改进 - 严重度高的可访问性问题]
2. [次高优先级改进 - 严重度高的功能性问题]
3. ...

### 🟡 中优先级（建议修复）
1. [中等优先级改进 - 用户体验提升]
2. ...

### 💡 低优先级（可选优化）
1. [低优先级改进 - 锦上添花]
2. ...

## 改进预估

| 优先级 | 问题数 | 预估工时 | 影响范围 |
|--------|--------|---------|---------|
| 高 | N | Xh | 全局/核心 |
| 中 | N | Xh | 局部 |
| 低 | N | Xh | 细节 |
| **合计** | **N** | **Xh** | — |
```

---

### 5. 改进执行

审查后可直接执行改进，形成审查-改进-验证的闭环。

#### 5.1 执行原则

- **审查不改代码**：review 子技能的职责是发现问题，不直接修改代码
- **生成返修清单**：每个高/中严重度问题形成结构化返修任务
- **发回 design 子技能**：返修清单通过 pass-data.yaml 传递给 design 子技能
- **增量 mini-review**：design 修复后，仅对涉及维度重新审查

#### 5.2 审查→设计返修流程

1. **生成返修清单**：汇总所有高/中严重度问题
2. **写入 pass-data.yaml**：
   ```yaml
   rework-tasks:
     - id: REV-001
       dimension: "排版"
       severity: "high"
       issue: "ALL CAPS 缺少 letter-spacing"
       location: "index.html:45"
       suggestion: "添加 letter-spacing: 0.08em"
     - ...
   ```
3. **发回 design**：携带返修清单调回 design 子技能
4. **design 修复后 → mini-review**：仅对返修涉及的维度重新审查
5. **验证闭环**：确认所有返修任务完成

#### 5.3 返修清单格式

```yaml
# pass-data.yaml 中的返修段
rework-tasks:
  - id: string        # REV-{序号}
    dimension: string  # 对应审查维度名称
    severity: high|medium
    issue: string      # 问题描述
    location: string   # 文件:行号
    suggestion: string # 具体修复建议
    status: pending|fixed|verified
```

#### 5.4 改进验证清单

每个维度改进后，验证以下内容：

- [ ] 所有高严重度问题已修复
- [ ] 改进未引入新问题
- [ ] 改进未破坏现有功能
- [ ] 改进在所有目标浏览器中表现一致
- [ ] 改进在暗色模式下表现正确（如适用）
- [ ] 改进在移动端表现正确（如适用）

#### 5.5 反AI味专项审查

> ⚠️ 硬检查见 §0.5-§0.6（审查前置阶段执行，Block 级别），本节为审查报告维度的汇总引用。

反AI味审查结果已包含在 §0.5（七宗罪硬检查）和 §0.6（软规则检查）中。审查报告维度表中的"反AI味"得分 = 七宗罪通过率 × 10。

- 全部 7 项通过 → 10/10
- 每 1 项 Block → 扣 2 分

#### 5.6 设计灵魂追溯检查

> 设计灵魂追溯的完整检查见 §0（审查前置）。本节为维度表汇总引用。
> 审查报告维度表中的"设计灵魂"得分 = §0 灵魂匹配度评分 / 10。

#### 5.7 改进后评分提升

改进后重新评分，记录评分变化：

```markdown
## 改进记录

| 维度 | 改进前 | 改进后 | 变化 |
|------|--------|--------|------|
| 排版 | 5/10 | 8/10 | +3 ⬆️ |
| 间距 | 4/10 | 7/10 | +3 ⬆️ |
| ... | ... | ... | ... |
| **总体** | **52/100** | **78/100** | **+26 ⬆️** |
```

#### 5.8 迭代改进

- 改进是一个迭代过程，可能需要多轮
- 每轮改进后重新审查，确认效果
- 当所有维度达到 ✅ 状态（≥8分）时，审查完成
- 最终输出改进总结报告
