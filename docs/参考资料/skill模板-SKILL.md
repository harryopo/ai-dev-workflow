---
# ================================================================
# 标识
# ================================================================

# Skill 名称和 /slash-command 名称
# 全小写+数字+连字符，≤64 字符
# 禁止包含 XML 标签、"anthropic"、"claude"
name: your-skill-name

# Skill 做什么 + 什么时候触发
# ≤1024 字符（列表中截断为 250 字符）
# 前 250 字符必须包含核心触发条件
description: |
  一句话描述 skill 的核心功能。
  当用户提到"关键词1"、"关键词2"、"关键词3"时触发。

# 参数提示，自动补全时显示给用户
argument-hint: "[参数说明]"

# ================================================================
# 触发控制
# ================================================================

# true = 只能用户手动 /name 触发
# description 不进入上下文，Claude 不知道此 Skill 存在
# 默认：false
disable-model-invocation: false

# false = 用户看不到此 Skill，只有 Claude 能调用
# 默认：true
user-invocable: true

# Glob 模式，只在处理匹配文件时才自动激活
# 逗号分隔或 YAML 列表
# paths: "*.md, src/**/*.ts"

# ================================================================
# 执行模式
# ================================================================

# fork = 在独立子 Agent 中运行（隔离，无对话历史）
# 省略 = 在当前会话中运行（inline）
# 复杂任务必须用 fork，这是上下文窗口的生存策略
context: fork

# context: fork 时使用的子 Agent 类型
# 内置：Explore | Plan | general-purpose
# 或 .claude/agents/ 中的自定义 Agent name
# 默认：general-purpose
agent: "general-purpose"

# Skill 激活时免询问的工具
# 空格分隔或 YAML 列表
allowed-tools: Read Write Edit Glob Grep

# 覆盖会话模型（可选）
# model: sonnet

# 覆盖会话 effort（可选）
# low | medium | high | max
# effort: medium

# ================================================================
# 生命周期
# ================================================================

# Skill 级钩子，与全局/Agent 级钩子隔离
# 支持：PreToolUse | PostToolUse | UserPromptSubmit | Stop
# hooks:
#   PostToolUse:
#     - matcher: "Write"
#       hooks:
#         - type: command
#           command: "echo 'done'"

# Shell 预处理命令使用的 shell：bash | powershell
# shell: bash
---

# Skill 标题

简要说明这个 skill 是做什么的。

## 触发条件

### 精确匹配
- 关键词1、关键词2、关键词3
- 用户提到 "xxx"、"yyy" 时
- 当任务包含 ZZZ 时

### 模糊匹配
- 任务属于 XXX 领域
- 需要 YYY 功能

### 不触发条件
- 任务与 XXX 无关
- 更简单的工具可以处理

## 核心原则

> **一句话总结核心理念。** 说明为什么需要这个 skill，它解决了什么问题。

## 工作流程

### 第一步：接收输入

1. **解析用户输入**
   - 使用 $ARGUMENTS 获取用户参数
   - 文字输入 → 直接解析
   - 文件路径 → 使用 Read 工具读取

2. **确认理解**
   - 向用户确认理解的意图
   - 必要时询问缺失信息

### 第二步：核心处理

1. **准备阶段**
   - 读取参考资料：`${CLAUDE_SKILL_DIR}/references/xxx.md`
   - 加载配置文件（如有）

2. **执行核心逻辑**
   - 步骤 1：{具体操作}
   - 步骤 2：{具体操作}
   - 步骤 3：{具体操作}

3. **生成结果**
   - 处理数据
   - 格式化输出

### 第三步：输出结果

1. **格式化输出**
   - 按照输出模板组织内容
   - 确保格式规范

2. **保存结果**
   - 保存到指定目录
   - 使用规范的文件名

3. **展示给用户**
   - 显示关键内容
   - 提供文件路径

## 输出规范

### 输出格式
- 格式类型：Markdown / JSON / 纯文本
- 编码：UTF-8

### 输出位置
- 目录：`D:\path\to\output\`
- 文件名格式：`{前缀}_{YYYYMMDD}.{扩展名}`

### 输出模板

```markdown
# {任务标题} - {日期}

## 任务描述
{用户的需求描述}

## 执行结果
{具体内容}

## 总结
{要点回顾}
```

## 参考资料

- 文件1：`${CLAUDE_SKILL_DIR}/references/file1.md` - {说明}
- 文件2：`${CLAUDE_SKILL_DIR}/references/file2.md` - {说明}

## 注意事项

### 必须遵守
- 规则1：{详细说明}
- 规则2：{详细说明}

### 建议遵循
- 建议1：{详细说明}
- 建议2：{详细说明}

### 禁止行为
- 禁止1：{详细说明}
- 禁止2：{详细说明}

## 限制条件

- 限制1：{说明}
- 限制2：{说明}

## 示例

### 输入示例
```
用户输入内容
```

**期望输出：**
```
期望的输出内容
```

## 错误处理

| 错误类型 | 错误信息 | 解决方案 |
|----------|----------|----------|
| 输入错误 | "XXX" | {解决方法} |
| 处理错误 | "YYY" | {解决方法} |

---

**提示：** 将此模板复制到 `~/.claude/skills/你的skill名/SKILL.md`，替换所有占位符为实际内容。
